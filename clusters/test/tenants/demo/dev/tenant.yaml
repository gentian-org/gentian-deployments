apiVersion: gentianos.io/v1alpha1
kind: Tenant
metadata:
  name: demo
spec:
  adminEmail: admin-demo@gentian.org
  displayName: Demo
  isolation:
    databasePrefix: demo_
    keycloakRealm: demo
    ldapOU: ou=demo
    s3Prefix: demo-
  deletionPolicy: Retain
  apps:
  - profile: element
    # Jitsi is bundled as an Element sidecar (see gentian-apps/profiles/element.yaml).
